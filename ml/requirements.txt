# ClaimVision ML — runtime dependencies
# Heavy packages (torch, ultralytics) are NOT installed during Phase 0
# to avoid multi-GB downloads. Install these before Phase 1 ML work begins.
#
# Install order:
#   1. pip install -e ml/          (installs claimvision_ml package)
#   2. pip install -r ml/requirements.txt   (installs runtime deps)
#
# GPU note: replace torch>=2.2 with the CUDA-enabled wheel for your hardware.

# Deep learning
torch>=2.2,<3
torchvision>=0.17,<1
timm>=1.0,<2

# Computer vision
opencv-python-headless>=4.9,<5
Pillow>=10.0,<12

# Augmentation
albumentations>=1.4,<2

# Object detection
ultralytics>=8.2,<9

# Evaluation and visualisation
pandas>=2.2,<3
scikit-learn>=1.4,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1
imagehash>=4.3,<5

# Utilities
pyyaml>=6.0
tqdm>=4.66
numpy>=1.26,<3
kagglehub>=0.2.0

