# ClaimVision ML — development dependencies
# Extends runtime requirements with testing, linting, and notebook tools.
# NOTE: does NOT include torch/heavy deps — install requirements.txt separately.

# Testing
pytest>=8.0
pytest-cov>=5.0

# Linting and formatting
ruff>=0.4

# Notebooks (Colab already provides these; install locally for development)
jupyter>=1.0
ipykernel>=6.0
nbformat>=5.9
nbconvert>=7.0
